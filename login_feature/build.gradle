apply from: "$rootProject.projectDir/android-dynamic-feature-common.gradle"

android {
    buildFeatures {
        dataBinding true // Since login_presentation uses dataBinding, need to set it here
    }
}

dependencies {
    implementation project(':app')
    implementation project(':login_presentation')
}